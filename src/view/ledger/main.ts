import Vue from "vue";

import Ledger from "./Ledger.vue";

const vm = new Vue({
	el: "#app",

	created: function (): void {

	},

	data: {

	},

	methods: {

	},

	components: {
		Ledger
	}
});
(window as any).vm = vm;
