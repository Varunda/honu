<template>
    <div>
        ye
    </div>
</template>

<script lang="ts">
    import Vue, { PropType } from "vue";
    import Chart, { LegendItem } from "chart.js/auto/auto.esm";

    import { randomRGB, rgbToString, randomColors, randomColor } from "util/Color";

    export const ChartPlayerControl = Vue.extend({
        props: {

        },

        data: function() {
            return {

            }
        },

        methods: {

        }
    });
    export default ChartPlayerControl;
</script>