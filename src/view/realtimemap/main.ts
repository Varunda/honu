import Vue from "vue";

import RealtimeMap from "./RealtimeMap.vue";

const vm = new Vue({
	el: "#app",

	created: function (): void {

	},

	data: {

	},

	methods: {

	},

	components: {
		RealtimeMap
	}
});
(window as any).vm = vm;
